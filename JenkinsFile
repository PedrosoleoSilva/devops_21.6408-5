pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    sh '''
                    java --version
                    docker version
                    docker-compose version
                    '''
                }
            }
        }
    }
}
